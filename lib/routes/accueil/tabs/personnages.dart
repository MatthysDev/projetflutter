import 'package:flutter/widgets.dart';

class PersonnesTab extends StatelessWidget {
  const PersonnesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Personnages');
  }
}